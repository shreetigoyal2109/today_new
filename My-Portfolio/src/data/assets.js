// Centralized image assets. If you have direct Canva image URLs, paste them here.
// Example: export const heroImage = 'https://example.com/your-canva-image.jpg'

// Set `heroImage` to your exported theme image (place the file in `public/images` as `theme.png`).
// The hero `<img>` has an onError fallback to `/images/Shreeti.jpg` if the file is missing.
export const heroImage = '/images/theme.png';

export const projectImages = {
  healthcare: '/images/healthcare.svg',
  digalu: '/images/digalu.svg',
  banking: '/images/banking.svg',
};

// If you want to use Canva-exported public URLs, replace the values above
// with the direct image URLs from Canva (export and host somewhere public).
